<%= form_for @partner, :html => {:multipart => true} do |f| %>
  <% if @partner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partner.errors.count, "error") %> prohibited this partner from being saved:</h2>

      <ul>
      <% @partner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :avatar %><br />
    <%= f.file_field :avatar %>
  </div>
  <div class="field">
    <%= f.label "Biografie voor in profiel" %><br />
    <%= f.text_area :biography, :class => "input" %>
  </div>
  <div class="field">
    <%= f.label "Korte beschrijving voor in overzicht" %><br />
    <%= f.text_area :description, :class => "input" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
